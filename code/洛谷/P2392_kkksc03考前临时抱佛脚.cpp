#include <bits/stdc++.h>
#include <algorithm>
#include <iomanip>

using namespace std;
int main() {
  int time;
  int s1, s2, s3, s4;
  cin >> s1 >> s2 >> s3 >> s4;
  vector<int> A(s1), B(s2), C(s3), D(s4);

  for (auto& i : A) {
    cin >> i;
  }
    for(auto &i:B){
      cin >> i;
    }
    for(auto &i:C){
      cin >> i;
    }
    for(auto &i:D){
      cin >> i;
    }

    while (D.rbegin()!=0)
    {
        
    }
    
}
