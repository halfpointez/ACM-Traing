#include <bits/stdc++.h>
using namespace std;
using i64 = long long;
#define endl '\n'
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int T;
	cin >> T;
	while (T--) {
		i64 n, k, q;
		cin >> n >> k >> q;
		vector<i64> a(n), b(n);
		for(i64 i = 0; i < n; i++) {
			cin >> a[i] >> b[i];
		}
	}
	
}