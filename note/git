# git命令  

这是一个git新手记不住命令而设置的本地快查文本

## 命令如下
  
#### 本地创建仓库
`git init` 
#### 用户信息
`git config --global user.name "xxx"`
`git config --global user.email xxx`
#### 文件操纵
`pwd` //显示会话位置路径
`ls` //显示目录下所有文件
#### 保存提交拉取
`git add .` //暂时保存变更
`git commit` //提交更改
`git commit -m "(提交信息)"` //提交但跳过编辑
`git push origin branch-name` //将本地版本库的提交推送到远程仓库分支。
`git push origin main` //将本地提交推送到远程仓库
`git pull origin branch-name` //拉取远程仓库内容到本地

#### 配置

`git config --list`//显示当前git配置